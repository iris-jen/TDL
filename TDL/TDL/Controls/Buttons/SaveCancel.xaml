<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             VerticalOptions="EndAndExpand"
             x:Class="SelfMonitoringApp.Controls.SaveCancel">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" CornerRadius="10" BorderWidth="5" BorderColor="#ff5722" BackgroundColor="#03a9f4" 
                        Margin="5" Text="Cancel" Command="{Binding PopCommand}"/>

        <Button Grid.Column="1" CornerRadius="10" BorderWidth="5" BorderColor="#ff5722" BackgroundColor="#03a9f4"
                        Margin="5" Text="Save" Command="{Binding SaveCommand}"/>
</Grid>